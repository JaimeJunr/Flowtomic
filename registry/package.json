{
  "name": "@flowtomic/registry",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun run build-registry.ts",
    "test": "vitest run",
    "type-check": "tsc --noEmit",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "format": "biome format --write .",
    "format:check": "biome format ."
  },
  "devDependencies": {
    "@types/bun": "^1.4.2",
    "vitest": "^2.1.8"
  }
}
